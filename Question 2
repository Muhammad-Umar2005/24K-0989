#include<stdio.h>
int main(){
 int a,b,z;
 printf("Enter the value of a,b\n");
  scanf("%d%d",&a,&b);
 printf("\nbefore swapping numbers:%d%d",a,b);
 z=a;
 a=b;
 b=z;
 printf("\nafter swapping numbers:%d%d",a,b);
 return 0;
}
